<UserControl x:Class="GestioneEsercizi.Views.EsercizioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestioneEsercizi.Views"
             xmlns:viewmodel="clr-namespace:GestioneEsercizi.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Riferimento dei dati preso da EsercizioViewModel -->
    <UserControl.DataContext>
        <viewmodel:EsercizioViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <!-- Titolo esercizio -->
        <TextBox x:Name="titoloTextBox" Grid.Column="0" Grid.Row="0" />
        <!-- Placeholder -->
        <TextBlock IsHitTestVisible="False" Text="Titolo esercizio..." 
                   VerticalAlignment="Center" Foreground="DarkGray" Margin="10 0 0 0">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=titoloTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <!-- Menu - Modulo, Tematica, Classe -->
        <ComboBox Grid.Column="1" Grid.Row="0" SelectedIndex="0" 
                  ItemsSource="{Binding Path=Moduli}"  />
        <ComboBox Grid.Column="2" Grid.Row="0" SelectedIndex="0" 
                  ItemsSource="{Binding Path=Tematiche}" />
        <ComboBox Grid.Column="3" Grid.Row="0" SelectedIndex="0" 
                  ItemsSource="{Binding Path=Classi}" />
        <!-- Testo esercizio -->
        <TextBox x:Name="testoTextBox" Grid.ColumnSpan="2" Grid.Row="1" Margin="10" />
        <!-- Placeholder -->
        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                   IsHitTestVisible="False" Text="Testo esercizio..." 
                   Margin="20 20 0 0" Foreground="DarkGray">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=testoTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <!-- Immagine -->
        <Border Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="1"
                BorderBrush="Black" BorderThickness="0.5" Margin="50"
                AllowDrop="True" Drop="ImageDrop">
            <Grid>
                <Label x:Name="immagineLabel" Content="Trascina immagine" />
                <Button x:Name="sfogliaButton" VerticalAlignment="Center" 
                HorizontalAlignment="Center" Content="Sfoglia..." Click="sfogliaButtonClick" />
            </Grid>
        </Border>
        <DockPanel Grid.Row="2" Grid.Column="3" Margin="10"
                   VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <!-- Pulsante salva -->
            <Button Content="Annulla" Command="{Binding Path=BenvenutoCommand}" />
            <!-- Pulsante indietro -->
            <Button Content="Salva" Margin="10 0 0 0" />
        </DockPanel>
    </Grid>
</UserControl>